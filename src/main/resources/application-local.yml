server:
    port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bootdb?allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

stock-manager:
    uri: localhost:8080/stock
    registrationUri: localhost:8080/notification
